import React, { Component } from "react";

class SearchBar extends Component {
  render() {
    return (
      <div>
        I am a search bar: topic | something else | something else entirely
      </div>
    );
  }
}

export default SearchBar;
